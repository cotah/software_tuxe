# Server
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3000,http://localhost:5173
APP_BASE_URL=http://localhost:3001

# Database
DATABASE_URL=postgresql://btrix:btrix123@localhost:5432/btrix_db

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Auth
JWT_SECRET=change-me-in-dev
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=30d

# Encryption (required for sensitive fields)
ENCRYPTION_KEY=dev-encryption-key-please-change

# SMTP (optional in dev; mock fallback used when empty)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@btrix.com

# Twilio (optional in dev; mock fallback used when empty)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=
TWILIO_PHONE_FROM=

# Logging
LOG_DIR=logs

# Google Calendar OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/api/calendar/google/callback

# Microsoft Calendar OAuth
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_REDIRECT_URI=http://localhost:3001/api/calendar/microsoft/callback
MICROSOFT_TENANT=common

# AI
AI_PROVIDER_API_KEY=
AI_MODEL=gpt-4.1-mini
OPENAI_API_KEY=
OPENAI_BASE_URL=
DEFAULT_AI_MODEL=gpt-4.1-mini

# Ingest mappings (comma separated: key:tenantId)
INGEST_TENANT_KEYS=demo-key:demo-tenant-id
